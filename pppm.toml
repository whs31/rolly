[package]
name = "leaf"
version = "0.3.5"
authors = ["whs31 <whs31@github.io>"]
description = "Core library of simulator namespace and beyond"

[dependencies]
cmake = { version = "0.1.2", distribution = "sources" }
fmt = { version = "10.1.1", distribution = "static" }
spdlog = { version = "1.13.0", distribution = "static" }