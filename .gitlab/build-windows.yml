build windows:
  tags: [windows]
  stage: build

  script:
    - mkdir -p target
    - cd target
    - mingw64-cmake -GNinja -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF ..
    - cmake --build .

  artifacts:
    paths: [ target/*.a ]
    expire_in: 1 hour
